<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/main.css" asp-append-version="true" />
</head>
<body>

   @* <div id="sidebar" class="sidebar">

        <div class="p-3 d-flex align-items-center justify-content-between border-bottom">
            <button id="menu-close" aria-label="Закрити меню" class="btn btn-light text-primary fs-4 p-0">☰</button>
            <span class="text-primary fw-bold">PastBeam</span>
        </div>

        <div class="p-3">
            <ul class="list-unstyled">
                <li class="mb-3"><a href="#" class="d-flex align-items-center text-primary gap-2 text-decoration-none">Main Page</a></li>
                <li class="mb-3"><a href="#" class="d-flex align-items-center text-primary gap-2 text-decoration-none">Your Folders</a></li>
                <li class="mb-3"><a href="#" class="d-flex align-items-center text-primary gap-2 text-decoration-none">Favorite</a></li>
                <li class="mb-3"><a href="#" class="d-flex align-items-center text-primary gap-2 text-decoration-none">Courses</a></li>
            </ul>
        </div>

    </div> *@

    <header class="main-header">
        @* Це новий початок хедера *@
        <div class="container-fluid d-flex justify-content-between align-items-center">

            @* Ліва частина хедера *@
            <div class="header-left d-flex align-items-center">
                @* Виріши, чи потрібна кнопка для бічної панелі. Якщо ні - цей <button> можна видалити.
                Якщо так - залиш, але на мокапі "Menu page" її не видно.
                <button id="menu-toggle" aria-label="Відкрити меню">☰</button>
                *@
                <a class="navbar-brand header-logo" asp-area="" asp-controller="Home" asp-action="Index">
                    PastBeam @* Поки що без іконки *@
                </a>
            </div>

            @* Центральна частина хедера *@
            <div class="header-center">
                <span class="header-title">Menu panel</span>
            </div>

            @* Права частина хедера *@
            <div class="header-right d-flex align-items-center">
                @if (User.Identity != null && User.Identity.IsAuthenticated)
                {
                    // Якщо користувач залогінений - показуємо поки що просто текст або посилання на профіль
                    <a asp-controller="Account" asp-action="Manage" title="Manage" class="nav-link text-dark">
                        Профіль @* Замість іконки поки текст *@
                    </a>
                    <form class="form-inline" asp-controller="Account" asp-action="Logout" method="post" style="margin-left: 10px;">
                        <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
                    </form>
                }
                else
                {
                    // Якщо користувач не залогінений - показуємо посилання Login/Register
                    <a class="nav-link text-dark text-decoration-none header-auth-link" asp-area="" asp-controller="Account" asp-action="Login">Login</a>
                    <a class="nav-link text-dark text-decoration-none header-auth-link" style="margin-left: 10px;" asp-area="" asp-controller="User" asp-action="RegisterUser">Register</a>
                }
            </div>
        </div>
    </header> @* Це кінець нового хедера *@
    <div class="main-container">

        <main role="main">
            @RenderBody()
        </main>

    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2025 - PastBeam.Presentation1 - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
